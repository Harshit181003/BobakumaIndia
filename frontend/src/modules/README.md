# Feature modules

Use this folder for larger vertical slices (e.g. `catalog/`, `checkout/`, `admin/`) — co-locate hooks, subcomponents, and feature-specific types.

The app currently implements most UI under `src/app` and `src/components`; migrate here when a feature outgrows flat folders.
